import 'package:flutter/material.dart';


class SKModelPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Colors.red,
      appBar: AppBar(
        title: Text("Model Page"),
      ),
      body: Center(
        child: Text("Model Page"),
      ),
    );
  }
}